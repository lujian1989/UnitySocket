The libzipw.a plugin is compiled for arm64 with no bitcode enabled.
If you need a bitcode compiled plugin decompress the one in the bitcode folder and replace libzipw.a.

There are compilations of the plugin that support also arm64/armv7.